import { css } from '@emotion/react'

export default {
  ribbon: css`
    clip-path: inset(0);
    transform: rotate(45deg);
    right: -33px;
    top: 12px;
    line-height: initial;
    text-align: center;
    p {
      line-height: initial;
    }
  `,
}
